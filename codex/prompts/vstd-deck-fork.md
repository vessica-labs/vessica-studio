---
description: Vessica Studio — deck-fork workflow (vstd)
---

Run `vstd skill deck-fork` and follow the instructions it prints exactly. They are
the canonical workflow for this task. If `vstd` is not on PATH, install it
first: `go install github.com/vessica-labs/vessica-studio/cmd/vstd@latest`.
Also run `vstd skill conventions` once per session and keep those authoring
conventions in mind throughout.

Task arguments from the user: $ARGUMENTS
