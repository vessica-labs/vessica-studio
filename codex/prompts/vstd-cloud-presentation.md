---
description: Vessica Studio — cloud-presentation workflow (vstd)
---

Run `vstd skill cloud-presentation` and follow the instructions it prints
exactly. They are the canonical workflow shipped by the installed `vstd`
engine. If `vstd` is unavailable, stop and ask the user to install or update it;
do not substitute a stale copied workflow.
